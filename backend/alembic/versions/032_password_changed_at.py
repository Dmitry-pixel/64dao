"""Момент последней смены пароля — для аннулирования старых токенов

Ссылка сброса пароля (JWT на час) была многоразовой: проверялись только
подпись, срок и тип. Сменив пароль, владелец не закрывал ни саму ссылку,
ни активные сессии — кука жила свои 7 дней независимо. То есть смена
пароля не решала задачу, ради которой её обычно и делают.

Колонка хранит момент смены. Токены сравниваются с ней по iat: всё, что
выпущено раньше, перестаёт действовать — и ссылка сброса, и сессии.

NULL означает «пароль не менялся ни разу». Существующие пользователи
получают именно NULL, поэтому проверка на них не срабатывает и никого не
разлогинивает при выкатке.

Revision ID: 032
Revises: 031
"""
import sqlalchemy as sa

from alembic import op

revision = "032"
down_revision = "031"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "users",
        sa.Column("password_changed_at", sa.DateTime(timezone=True), nullable=True),
    )


def downgrade() -> None:
    op.drop_column("users", "password_changed_at")
