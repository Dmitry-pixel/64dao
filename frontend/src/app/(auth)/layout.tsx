// frontend/src/app/(auth)/layout.tsx
// Auth layout — страницы занимают весь экран, никаких обёрток

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
