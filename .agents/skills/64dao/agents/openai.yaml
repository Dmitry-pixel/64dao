interface:
  display_name: "64dao"
  short_description: "Repo-specific patterns and workflows for 64dao"
  default_prompt: "Use the 64dao repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true